// Assets declared here preload before resources' "ready" fires, then live
// in experience.resources.items keyed by name. Put the files in static/.
// shred is fully procedural — no assets.
export default [];
